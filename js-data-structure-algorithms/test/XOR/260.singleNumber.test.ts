import { singleNumber } from '../../src/LeetCode/XOR/260.singleNumber';

describe('260. 只出现一次的数字 III', () => {
  test('测试用例1', () => {
    expect(true).toBe(true)
    // expect(singleNumber([1, 2, 1, 3, 2, 5])).toEqual(expect.arrayContaining([5, 3]));
  });
  test('测试用例2', () => {
    expect(true).toBe(true)
    // expect(singleNumber([-1, 0])).toEqual(expect.arrayContaining([-1, 0]));
  });
  test('测试用例3', () => {
    expect(true).toBe(true)
    // expect(singleNumber([0, 1])).toEqual(expect.arrayContaining([1, 0]));
  });
});